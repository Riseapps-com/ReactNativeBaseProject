import { storiesOf } from '@storybook/react-native';
import React from 'react';

import ActivityIndicator from '../index';

storiesOf('ActivityIndicator', module).add('with data', () => <ActivityIndicator />);
